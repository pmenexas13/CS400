// create function that takes a value and prints its cube
const printCube = val => console.log(val**3);

// store test case list in variable
const list1 = [1,2,3,4,5,6,7];

// run function on each element of list using map
list1.map(printCube)